name: Build APKs on README Update

on:
  push:
    branches:
      - '**'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Make gradlew executable
        run: chmod +x gradlew

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: 17
          distribution: "zulu"
          cache: 'gradle'

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v2

      - name: Accept licenses
        run: yes | sdkmanager --licenses

      - name: Install NDK and CMake
        run: |
          sdkmanager "ndk;26.1.10909125" "cmake;3.22.1"

      - name: Build APKs (32bit + 64bit + universal)
        run: ./gradlew assembleRelease

      - name: Create GitHub Release & Upload APKs
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v1.4
          name: "🔥 Final Update v1.4 – Open Source Release"
          body: |
            🎉 **ROCK Discord Selfbot – Final & Open Source Release v1.4**

            **🆕 What's New:**
            - 🌍 The project is now **100% Open Source!**
            - 🚫 Removed Login Screen – direct and lightweight experience
            - 🐞 Fixed multiple minor bugs and crashes
            - ⚡ Improved build speed and performance
            - 💬 Updated README + legal info + discontinued notice
            - 🛠️ Stable and clean final version

            💡 *Note:* This is the **final and discontinued** update for ROCK Discord Selfbot APK.  
            The project is open for the community — fork it, modify it, and keep it alive! ❤️

            👑 Developer: Ron
          files: |
            app/build/outputs/apk/release/app-armeabi-v7a-release.apk
            app/build/outputs/apk/release/app-arm64-v8a-release.apk
            app/build/outputs/apk/release/app-universal-release.apk